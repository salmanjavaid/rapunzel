{
  "scripts": {
    "build:webui": "esbuild webui/src/app.js --bundle --format=esm --outfile=webui/dist/app.js"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "esbuild": "^0.28.0"
  }
}
